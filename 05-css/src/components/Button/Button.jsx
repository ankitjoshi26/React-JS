import React from 'react';
import styles from './Button.module.css';

const Button = () => {
  return (
    <div className={styles.btn}>
      <button>Button from Button</button>
    </div>
  );
}

export default Button;
